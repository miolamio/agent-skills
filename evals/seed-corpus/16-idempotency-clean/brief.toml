[meta]
schema_version = "1.0"
genre = "internal"
intensity = "light"
mode = "line_edit"
expected_mode = "proofread"

[expected_findings]
# Edge case: Idempotency — source.md is already clean. expected.md is byte-identical; linter produces 0 findings on both.
# Linter on source.md: hard_fail=0, warn=0.
# Linter on expected.md: hard_fail=0, warn=0.

hard_fail_min = 0
hard_fail_max = 0
warn_min = 0
warn_max = 0

# HARD_FAIL checks that fired on source.md (must be eliminated in expected.md):
checks_must_fire = []

# These must NOT fire on expected.md (acceptance gate ensures hard_fail=0):
checks_must_not_fire_on_expected = []

# Idempotency: lint(expected.md) must produce 0 HARD_FAIL.
expected_clean_on_lint = true
