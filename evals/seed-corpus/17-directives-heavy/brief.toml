[meta]
schema_version = "1.0"
genre = "internal"
intensity = "heavy"
mode = "line_edit"

[expected_findings]
# Edge case: Directive-heavy — wrapped sections contain banned markers suppressed by ru-lint:ignore-* directives. Unwrapped sections are clean.
# Linter on source.md: hard_fail=0, warn=3.
# Linter on expected.md: hard_fail=0, warn=3.

hard_fail_min = 0
hard_fail_max = 0
warn_min = 2
warn_max = 5

# HARD_FAIL checks that fired on source.md (must be eliminated in expected.md):
checks_must_fire = []

# These must NOT fire on expected.md (acceptance gate ensures hard_fail=0):
checks_must_not_fire_on_expected = []

# Idempotency: lint(expected.md) must produce 0 HARD_FAIL.
expected_clean_on_lint = true
